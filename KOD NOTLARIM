data.tail -> "Bu bir tail fonksiyonudur" (Sadece bilgi verir).

data.tail() -> "Bu fonksiyonu hemen çalıştır ve bana verinin sonunu göster" (İşlemi yapar).

import → içe aktarmak (kütüphane eklemek)

socket → soket (ağ bağlantısı için yapı)

json → JSON formatı (veri değişim biçimi)

client → istemci (sunucuya bağlanan taraf)

server → sunucu (istekleri karşılayan taraf)

AF_INET → IPv4 adres ailesi

SOCK_STREAM → TCP bağlantısı (güvenilir, akış tabanlı)

connect → bağlanmak

ip → internet protokol adresi (ör. 127.0.0.1)

port → bağlantı noktası (ör. 5000)

request → istek (sunucuya gönderilen veri)

send → göndermek

encode → metni byte dizisine dönüştürmek

recv → almak (sunucudan veri çekmek)

data → veri

decode → byte dizisini tekrar metne çevirmek

loads → JSON string’i Python sözlüğüne dönüştürmek

dumps → Python sözlüğünü JSON string’e dönüştürmek

response → yanıt (sunucudan gelen cevap)

result → sonuç

close → kapatmak (bağlantıyı sonlandırmak)

import → içe aktarmak (kütüphane eklemek)

global → değişkeni fonksiyon dışında da kullanılabilir hale getirir

def → fonksiyon tanımlamak için kullanılır

return → fonksiyonun dışarıya değer döndürmesini sağlar

if / elif / else → koşul ifadeleri (eğer / değilse / aksi halde)

while True: → sonsuz döngü (durana kadar sürekli çalışır)

for → belirli sayıda döngü yapmak için kullanılır

print() → ekrana yazdırır

time.sleep(5) → programı 5 saniye bekletir

data["Close"].diff() → kapanış fiyatlarının farkını alır

.rolling(window=...) → hareketli ortalama hesaplamak için pencere açar

.mean() → ortalama değer hesaplar

.ewm(span=...) → üstel hareketli ortalama hesaplar

.iloc[-1] → son satırı seçer

.tail() → son birkaç satırı gösterir

and / or → mantıksal bağlaçlar (ve / veya)

< , > , == → karşılaştırma operatörleri (küçük, büyük, eşit)
len= UZUNLUK 
